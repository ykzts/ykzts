# pnpm configuration
# See: https://pnpm.io/npmrc

# Minimum release age for packages (in hours)
# Prevents installing packages that were published too recently
# This provides a safety layer similar to Renovate's minimumReleaseAge setting
# Renovate uses 3 days; this uses 24 hours for faster manual updates
minimum-release-age=24
